# Security policy

Please report vulnerabilities privately through GitHub's security advisory interface. Do not include secrets or customer data in reports.

The header identity adapter is intentionally a local demonstration boundary. In production, replace it with verified OIDC/JWT claims at a trusted gateway. Never expose this demo directly to an untrusted network.
